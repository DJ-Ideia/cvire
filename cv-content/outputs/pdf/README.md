# outputs/pdf

PDFs de currículo **gerados pelo app** (`exportService` / botão Export PDF), quando o usuário pedir para versionar o arquivo no repositório.

Não gerar PDF com biblioteca nova a partir dos prompts. Fluxo canônico: importar JSON → exportar no cvire → salvar aqui se necessário.
