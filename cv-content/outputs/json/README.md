# outputs/json

JSON no schema `CVProfile` (`src/types/cv.ts`), importável via `backupService.importResumesJSON` / UI do app.

Inclui samples (ex.: `modelo-curriculo-cvire-ia.json`, perfis PT/EN) e artefatos gerados por adaptação/tradução (`<slug>-adapted.json`, `<slug>-en.json`, etc.).
